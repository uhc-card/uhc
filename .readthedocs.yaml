version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

sphinx:
  configuration: docs/conf.py   # 👈 tells RTD to build from /docs

python:
  install:
    - requirements: requirements.txt
